{
  "name": "express-angular",
  "version": "0.0.1",
  "description": "This is a demo project showing a minimal example of an express server hosting an Angular website.",
  "scripts": {
    "start": "npm run build && node dist/app.js",
    "build": "npm run build-server && npm run build-client",
    "build-server": "tsc",
    "build-client": "cd src/client && npm run build && cd ../.."
  },
  "keywords": [
    "Angular",
    "express",
    "TypeScript"
  ],
  "author": "Jan-Niclas Struewer",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.0"
  }
}
